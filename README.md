# Notification-Modal

This modal is used for Google Tag Manager. A custom html tag is create with this html to display a notification banner. 

There are configurations that will hide/show it based on whether its set to true or false.
